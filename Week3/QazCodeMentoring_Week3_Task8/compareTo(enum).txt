Рассмотрев разные варианты реализации синглтонов, выводы таковы:
1. Оба ленивые
2. Enum изначально защищен от сериализации, а свой класс требует readResolve
3. Enum не подвержен рефлективным атакам, а в классе так-то можно сбросить и bool isInstanceCreated в фолс
4. Enum сильно проще в реализации public enum Singleton { INSTANCE; }

Отдельный класс, как в задании, нужен только при необходимости наследования другого класса 